# Real Estate Properties Livewire

Livewire 4 adapter for authorized, team-scoped property search. Public state
contains only the search term; the component delegates data access through the
core model boundary and renders explicit empty and accessible states.
